codes for MySQL databases are commented out

for sample database run the sql.py

Create .env file and add GOOGLE_API_KEY

run the app using command: streamlit run app.py

BTW you can't use streamlit in colab
